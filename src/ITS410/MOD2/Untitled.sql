/* 1. Write a SELECT statement that returns four columns from the Products table:
 product_code, product_name, list_price, and discount_percent. 
 Then, run this statement to make sure it works correctly. 
 Take a screenshot of the query and results. */
 #Grabs all columns
SELECT * FROM products;
# Grabs only product_code, product_name, list_price, discount_percent
SELECT product_code, product_name, list_price, discount_percent FROM products;
 
 
 